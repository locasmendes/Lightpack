# Pendências conscientes — Prismatik 5.17.0.0

Follow-ups deliberadamente fora do merge de `integrate/5.17` → `master`, ou só parcialmente fechados no tip (`7d56c8fd` e ancestrais Fases 1–6). Não são bugs esquecidos: são limites de escopo ou QA ainda pendente.

Plano de origem: `~/.claude/plans/adicione-um-modo-de-silly-hippo.md`.

## Testes / golden

| Item | Estado | Notas |
|---|---|---|
| Matriz golden parcial | Parcial | ~28 settings × ~45 cores via rows+loop — não o produto combinatório 40×50 do plano. Ver cabeçalho de `ColorPipelineGoldenTest.cpp`. |
| Tolerâncias full-chain temp+perceptual | Acima do alvo | Plano mirava ≤6 códigos 8-bit; medido ~19 no pior caso da matriz. Bound atual =20 (ordem B2/B3 vs legacy WP-antes-dos-ops). Content-stage e temp-only ficam mais apertados. |
| QBENCHMARK | Ausente | Sem microbenchmarks de pipeline/`calculations` nesta release. |
| Smoke runtime `QList<LinearRgbF>` / metatype | Código OK; QA runtime aberto | `qRegisterMetaType` em `ColorOps.cpp` + `LightpackApplication.cpp`. Falta smoke manual/CI que emita por `Qt::QueuedConnection` real e confirme frames sem warning de metatype. |

## QA manual / hardware

| Item | Estado |
|---|---|
| Topologia multi-tela (cenários Fase 1 / checklist) | Não executado nesta finalização |
| Foto 3 cenas Adalight + device UDP (antes/depois) | Pendente |
| Calibração (padrões, sessão longa, sem timeout) | Pendente |
| Live colors vs LED físico | Pendente |
| UI DPI 100/125/150/200% + temas claro/escuro + troca de idioma | Pendente |

Checklist multi-tela: [`phase-1-multiscreen-manual-checklist.md`](./phase-1-multiscreen-manual-checklist.md).

## Traduções

| Locale | unfinished (aprox. no tip) |
|---|---|
| `pt_BR.ts` | ~5 (prioridade deste fork; quase fechado) |
| `en.ts` | ~367 |
| `uk_UA.ts` | ~299 |
| `ru_RU.ts` | ~253 |
| `zh_CN.ts` | ~251 |
| `pl_PL.ts` | ~211 |

`lupdate` rodou na integração; strings novas da IA/pipeline ainda incompletas fora de `pt_BR`.

## Pipeline / device (follow-ups de código)

| Item | Onde / próximo passo |
|---|---|
| Dither Lightpack ainda off | `LedDeviceLightpack.cpp` — quantize 12-bit sem dither; ligar após QA HW (float tornaria dither efetivo pela 1ª vez). |
| Remover `Grab/Gamma` e `Device/Gamma` do disco | Migrados para `Device/OutputGamma`; chaves legadas ainda lidas na migração. Remoção física na **release seguinte**. |
| Média por zona ainda sRGB | `calculations.cpp` — `// TODO(linear-average):` (viés de Jensen). Linearizar por pixel fica para depois. |
| Layering Mood sobre grab | Só documentado na Fase 6 (`docs/efeitos-dinamicos-propostos.md`); sem implementação. |
| Fundir Saturação + Vibrance | Adiado (UX deliberada). |
| Precomputar índice de permutação `colorSequence` (Adalight/Ardulight) | Follow-up ~5 linhas, fora do pipeline. |
| Captura HDR / WGC | Explicitamente fora de escopo 5.17. |
| Firmware Hyperion parity | Plano separado; não nesta release. |

## Docs / processo

- Release notes binários em `releases/5.17.0.0/` são artefato local (gitignored) — não entram no git.
- Plano 5.17 continua externo (`silly-hippo`); este arquivo é o checklist versionado do que ficou para depois.

## Critério para fechar itens

Preferir: (1) QA HW + fotos, (2) smoke queued metatype, (3) apertar golden / documentar bound B2/B3 se permanente, (4) completar locales, (5) dither Lightpack + purge de chaves Gamma antigas na próxima minor/patch.
